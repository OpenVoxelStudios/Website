import React from 'react'
import ReactDOM from 'react-dom/client'
import When2Cook from '../When2Cook.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <When2Cook />
  </React.StrictMode>,
)
